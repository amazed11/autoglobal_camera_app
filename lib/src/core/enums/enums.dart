enum OTPFrom { loginOTP, registerOTP, forgotpassword, resendForgotOTP }

enum LoggerType {
  verbose,
  info,
  warning,
  error,
  debug,
  success,
}

enum Environment {
  production,
  staging,
  localhost,
}

enum CameraPurpose {
  cover,
  exterior,
  interior,
  other,
}

enum CameraOrientation {
  portraitUp,
  portraitDown,
  landscapeLeft,
  landscapeRight
}
